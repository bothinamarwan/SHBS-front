import { Routes } from '@angular/router';
import { LandlordDashboard } from './pages/landlord-dashboard/landlord-dashboard';

export const landlordRoutes: Routes = [
  { path: '', component: LandlordDashboard }
];
